<resources>
    <!-- TODO usare il tema di sistema e fare in modo di rendere dinamico l'inverted -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/app_background</item>
        <item name="android:colorAccent">@color/app_accent</item>
        <item name="android:colorPrimaryDark">@color/app_background_dark</item>

        <item name="colorPrimary">@color/app_background</item>
        <item name="colorAccent">@color/app_accent</item>
        <item name="colorPrimaryDark">@color/app_background_dark</item>
    </style>
</resources>
